# Sample output

`AD-ATLAS.csv` demonstrates the complete output schema using synthetic names and the reserved `company.com` example domain.

A real execution creates exactly one CSV with the same three columns. Never commit a real organization report to a public repository.
